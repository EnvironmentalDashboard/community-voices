{
    "postLogin": {
        "notation": "/authenticate",
        "defaults": {
            "resource": "Identification",
            "action": "postCredentials",
            "method": "post"
        }
    },

    "getLogin": {
        "notation": "/login",
        "defaults": {
            "resource": "Identification",
            "action": "getLogin",
            "method": "get"
        }
    },

    "getLogout": {
        "notation": "/logout",
        "defaults": {
            "resource": "Identification",
            "action": "getLogout",
            "method": "get"
        }
    },

    "getProfile": {
        "notation": "/user/me",
        "defaults": {
            "resource": "User",
            "action": "getProfile",
            "method": "get"
        }
    },

    "getProtectedPage": {
        "notation": "/protected",
        "defaults": {
            "resource": "User",
            "action": "getProtectedPage",
            "method": "get"
        }
    },

    "getQuote": {
        "notation": "/quote/{id}",
        "defaults": {
            "resource": "Quote",
            "action": "getQuote",
            "method": "get"
        }
    }
}
